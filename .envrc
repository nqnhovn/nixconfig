# ~/.config/nixos/.envrc
# Recommended method to integrate devenv with direnv.
# This will source the environment exports from devenv.
# Ensure devenv is installed globally or available in PATH.
eval "$(devenv shell --eval-cache 'echo $direnv_export_fn')"
