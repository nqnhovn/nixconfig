# ── Devenv / Direnv ───────────────────────────────────────────────
devbox.lock
.direnv/
.devenv/
result
result-*
*.pyc
__pycache__
.direnv
*.swp
*.swo
*~
.env
.env.local

# ── Secrets ────────────────────────────────────────────────────────
# TẤT CẢ key thật — CHỈ .example được tracked
# info.nix: thông tin người dùng (gitUser, email, fullName)
secrets/info.nix

secrets/keys.nix
secrets/aichat-keys.nix
secrets/*-keys.nix
*.key
*.pem
!*.example.nix
!*.example

# ── RAG cache ──────────────────────────────────────────────────────
*.rag-cache/

# ── Nix build artifacts ────────────────────────────────────────────
result-*
/nix/
/nix/var/
*.lock.chroot

# ── IDE / Editor ───────────────────────────────────────────────────
.zed/
*.swp
*.swo
*~
.DS_Store
